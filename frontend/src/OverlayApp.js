import React from 'react';

const OverlayApp = () => {
    return (
        <div className="min-h-screen bg-transparent flex items-center justify-center pointer-events-none">
            {/* Overlay content placeholder */}
        </div>
    );
};

export default OverlayApp;
