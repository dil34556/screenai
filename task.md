# Tasks

- [ ] Verify `framer-motion` is installed (Checked package.json).
- [ ] Modify `AdminLayout.js`:
    - [ ] Import `motion` and `AnimatePresence`.
    - [ ] Wrap the main container or specific elements with `motion.div` and `layout` prop.
    - [ ] Add route transition animations to the `Outlet` or a wrapper around it.
- [ ] Verify smoothness.
